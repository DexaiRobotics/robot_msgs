package robot_msgs;

struct joint_pos_cmd_t
{
    // microseconds since the epoch
    int64_t  utime;
    int32_t  num_joints;
    int32_t  num_confs;
    boolean  wait_for_ack;   
    double   cmd_joint_positions[num_confs][num_joints];
}