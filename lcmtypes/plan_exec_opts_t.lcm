package robot_msgs;

struct plan_exec_opts_t
{
    // Each plan exec option is a boolean flag corresponding to one bit
    const int16_t DEFAULT=0, MOVE_UNTIL_STOP=1, RETRACT_FROM_STOP=2, OFFSET_TRAJ=4;
}
